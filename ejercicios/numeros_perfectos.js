

const esPerfecto = (n) => {

    // Crear contador para comprobar cada numero de 1 a n-1
    // Crear suma para guardar la suma de los divisores

    // Inicio: Crear ciclo desde contador 1 a n-1 
        // Comparar n % contador es igual a 0
            // Si: Suma acumulativa con contador
        // Incrementar contador en uno
    // Fin: Finalizar ciclo con contador

    // Condicion

}